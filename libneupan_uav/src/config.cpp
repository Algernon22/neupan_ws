#include "neupan_uav/config.hpp"

namespace neupan_uav {

// PlannerConfig is currently a value-only contract type. Keep this translation
// unit so future YAML/metadata parsing can land without changing target shape.

}  // namespace neupan_uav
